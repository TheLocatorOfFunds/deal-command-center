/**
 * Placeholder for the ⊕ Quick FAB "tab."
 *
 * The FAB isn't a real navigable tab — the tabBarButton override in
 * (tabs)/_layout.tsx opens a sheet instead. expo-router requires a
 * route file for every Tabs.Screen, hence this no-op default export.
 */

export default function QuickPlaceholder() {
  return null
}
