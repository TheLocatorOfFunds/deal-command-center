<task-summary>
# On-demand court-pull poller — complete

**Shipped end-to-end integration** between DCC's UI button and Castle scrapers:

## What's now live
1. **Atomic claim RPC** (`claim_court_pull_request()`) deployed to DCC Supabase
2. **`utils/court_pull_poller.py`** — drains queue, uploads PDFs to `deal-docs/{deal_id}/`, triggers Claude Vision extraction, inserts events with `is_backfill=false`
3. **Butler CV3 + Franklin full PDF support** — every clickable doc on the docket gets downloaded and analyzed

## Verified with Casey Jennings (sf-jennings-moa9iqzt)
- 12m 26s end-to-end (Butler CV 2022 08 1416)
- **42 documents** uploaded → **47/47 extractions complete** (Claude Vision 100%)
- **77 new docket events** classified (disbursement_paid, motion_filed, etc.)
- **0 failures**

Documents tab should be fully populated; Case Intelligence card has real material.

## Deployment
```bash
# Run once (cron every 30s)
python -m utils.court_pull_poller --once --max 5

# Or daemon mode
python -m utils.court_pull_poller --daemon --interval 30
```

Migration file saved: `sql/migrations/20260422_claim_court_pull_request_rpc.sql` — DCC Claude should mirror to their repo.

**Queue currently empty** (2 done, 1 old failed, 0 queued). Ready for next button-click.
</task-summary>