**Summary for Archive**

Session spanned April 20-21, 2026 (turns 1-182). Owner: Nathan. Major thrust was Phase 1 of "Docket Guard" build-out for the Castle v2 (refundlocators-pipeline) project.

**Core milestones:**
1. Git init + first push to `github.com/TheLocatorOfFunds/refundlocators-pipeline` (later renamed to `castle-v2`)
2. Butler County CourtView 3 calibration — reCAPTCHA v2 migration handled
3. Built `solve_courtview_gate()` helper — unblocks 60 CV3 counties
4. Switched monitor_mode from direct Supabase writes to HMAC webhook path (matching DCC integration contract)
5. Built backfill CLI: `--deal-id` + `--backfill-days` for seeding client portals with case history
6. Verified end-to-end flow on John Dunn case (CV 2024 10 2117, Butler County) — 53 events backfilled, 5 critical
7. Wrote comprehensive handoff docs: `DCC_GO_LIVE_HANDOFF.md` + `SCRAPER_BUILDOUT_ROADMAP.md`

**Gotchas:**
- Butler's clerk site migrated from image CAPTCHA → reCAPTCHA v2; same migration likely across all 60 CV3 counties
- `utils/__init__.py` didn't auto-load `config/.env` — fixed
- CV3 fleet sweep harness had multiple hanging-worker issues; pivoted to subprocess-per-county model
- Eric's SOP mis-categorized Summit County as CV3 (actually uses `.asp` endpoint, not CourtView)

**Blockers identified (not resolved in-session):**
- DCC Edge Function still returns 401 (needs `--no-verify-jwt` redeploy)
- `DOCKET_WEBHOOK_SECRET` + `SUPABASE_SERVICE_KEY` not yet in Castle's `config/.env`

**End state:**
- 9 commits on `castle-v2` main branch
- Franklin + Butler counties live
- Infrastructure ready: classifier, webhook client, monitor_mode, backfill CLI
- DCC portal wiring confirmed (all 3 portals render docket events in realtime)
- 86 counties remaining (roadmap mapped to 5 phases)

**Token efficiency fixes applied:**
- Added 73-line `CLAUDE.md` primer to auto-load on new sessions
- Updated roadmap to reference CLAUDE.md instead of forcing 4 large-doc reads
- Added project `.claude/settings.local.json` for common-op allowlist

**Follow-ups punted to next session:**
- Phase 1 CV3 fleet sweep (59 counties) — subprocess harness written but not run
- Hamilton + Warren calibrations (Phase 2)
- Cuyahoga + Montgomery PROWARE (Phase 3)