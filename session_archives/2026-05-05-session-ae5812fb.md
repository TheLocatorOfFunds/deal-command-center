Perfect. Let me translate your answers into the next session's constraints:

## Vendor access (1, 2) — **Web automation, not API calls**

| Service | Method | Why |
|---|---|---|
| **PropStream** | Browser automation (Playwright/Selenium) via your VA's login | No API exists. Free once you have subscription. |
| **IDI Core** | Browser automation via your VA's login **OR skip it entirely** | API exists but costs $1.75/lookup. Since PropStream skip-trace is already free, recommend skipping IDI Core unless PropStream comes up empty on contact info. |

**Impact:** Agent needs headless browser capability (Playwright recommended over Selenium). Adds complexity + CAPTCHA handling but saves $1.75/lead. The `2Captcha` line item in CLAUDE.md stays.

---

## Rejected leads (3) — **Never enter DCC**

Just log to `research_rejections` table in Ohio-intel's DB (not DCC's). The DCC migrations doc already stubs this. Eric audits rejections in Ohio-intel's UI, not DCC.

---

## Multi-claimant fee (6) — **One deal, split among claimants**

**What you said:** "If we collect 100% of the surplus we get 25% whether it's 2 or 4 family members."

**How the agent builds it:**
- **One deal row** in DCC (not 4)
- **Four contacts + relationships** (spouse, 3 kids)
- **Four personalized URLs** (one per claimant) — each slug has their name, but all point to the same case
- **Tier/floor check happens on the case total**, not per-person
- **Fee is 25% of whatever total gets collected**, split internally among however many sign

The brief already had this right for the most part, but now confirmed.

---

## API failure mode (9) — **Push to human review**

PropStream down → log `reason_code: external_api_unavailable`, deal lands in `needs_human_review` queue for Eric. Don't silently skip.

---

## Phone confirmation (5) — **Twilio Lookup (automated) + Eric voice call (human)**

Agent runs Twilio Lookup to verify the line is active/valid. If valid, deal proceeds to DCC. Eric still makes the actual cold-call before Day-0 SMS goes out. Automated check just eliminates disconnected numbers early.

---

## Email (10) — **Best-effort (don't block on it)**

If PropStream returns an email, great — write it to `meta.homeownerEmail`. If not, still approve the deal. Email is a nice-to-have, not a gate.

---

## Budget (7) — **$1.75/lead max, prefer $0**

| Cost | When |
|---|---|
| **PropStream skip-trace** | Free (you already pay subscription) |
| **IDI Core** | $1.75 — **only use if PropStream returns no phone** |
| **Claude API tokens** | ~$0.02/lead (deep-dive uses Claude to parse scraped docs) |
| **Twilio Lookup** | ~$0.005/lead |
| **2Captcha** | ~$0.003/solve (occasional CAPTCHA on PropStream/court sites) |

**Target:** ~$0.03/lead when PropStream has good data, max $1.78/lead when it doesn't and you need IDI Core. At 100 leads/mo → $3–$178/mo new line item.

**Design constraint:** Agent tries PropStream first. Only escalates to IDI Core if PropStream returns no phone or invalid phone per Twilio Lookup.

---

## Where agent runs (8) — **Webhook-fired**

Ohio-intel POSTs to the agent's endpoint immediately after surfacing a new lead. Agent processes in real-time (within seconds). Lives on defender-mini or wherever you want to host a small Python service that can listen for webhooks.

**Needs from you:** confirm defender-mini has Python 3.11+ + can run a persistent HTTP service (Flask/FastAPI). If not, recommend a $5/mo VPS.

---

## Updated blockers this resolves

| Blocker from GAPS doc | Your answer resolved it? |
|---|---|
| PropStream API key | ✅ No API — web automation instead |
| IDI Core API access | ✅ Have login, skip the $1.75 API |
| Failure-mode policy | ✅ Push to human review |
| Email sourcing | ✅ Best-effort |
| Cost ceiling | ✅ Target <$0.05/lead |
| Where agent runs | ✅ Webhook on defender-mini |

**Still open (these aren't question-answerable — they're build tasks):**
- `markPrepped` frontend-JS issue (DCC's session fixes this)
- `ohio_case.sale_price` NULL issue (Ohio-intel#11 fixes this)
- `already_claimed` disqualifier (agent implements)
- Idempotency (agent implements)
- HMAC webhook signing (agent implements)

---

Want me to **write an updated `docs/CONSTRAINTS_FROM_SESSION.md`** capturing your answers verbatim so the next session doesn't have to infer? Would live alongside GAPS_AND_BLOCKERS.md and make the handoff even tighter.